Add the missing supporting files for this project to be deployed and version controlled properly. Go one file at a time and confirm with me before moving to the next.

0. First, verify the file layout: this is a monorepo with backend/ and frontend/ at the project root. main.py, requirements.txt, resume_tailor/, tests/, and pyproject.toml live inside backend/, as siblings of resume_tailor/ within that folder (not nested inside it). README.md and CLAUDE.md stay at the repo root. Fix the layout first if it's wrong, and correct any imports that break as a result, before touching anything else.

1. Create a backend/.env.example listing all environment variables referenced across claude_client.py, drive_client.py, docs_client.py, auth.py, and config.py, with placeholder values and short comments explaining each one.

2. Create a .gitignore at the repo root covering backend/.env, __pycache__, backend/.venv, *.pyc, credentials.json, any OAuth token files typically generated by Google API auth, and standard Python/IDE junk.

3. Check config.py for whether it already loads from .env using python-dotenv. If not, update it to do so.

4. Create a setup.py or pyproject.toml so the package is pip installable locally.

5. Add a tests/ folder scaffolded with basic test files matching the modules in resume_tailor. Mock all Google and Anthropic API calls rather than hitting real endpoints, tests must be able to pass without live credentials.

6. Inside backend/, create a virtual environment (using uv if available, otherwise python -m venv) and install the dependencies listed in requirements.txt into it.

7. From inside backend/, run a syntax/import check across the project (python -m py_compile on every .py file, plus a dry-run import of resume_tailor.pipeline) and run the test suite. Confirm both pass.

8. Check the current backend/.env against backend/.env.example and tell me exactly which fields are still empty and need real credentials. Don't fill these in or guess values.

Do not attempt to run main.py end to end at any point in this process, it requires live Google OAuth with a browser popup that needs to be completed manually.